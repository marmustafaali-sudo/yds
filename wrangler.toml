name = "yds"
compatibility_date = "2026-09-12"

# Statik site (Post üretici + words.json çalışma uygulaması). Sunucu tarafı
# Worker kodu yok. Web dosyaları zaten repo kökünde (index.html, app.js,
# post.js, ...) — build adımı GEREKMİYOR, doğrudan kökten deploy ediliyor.
# İstenmeyen dosyalar (node_modules, android/, ...) .assetsignore ile hariç.
[assets]
directory = "."
not_found_handling = "404-page"
